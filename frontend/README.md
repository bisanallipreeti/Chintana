# Chintana Vite Frontend

## Setup

1. Install dependencies:

```bash
npm install
```

2. Copy `.env.example` to `.env`.

3. Set API base URL in `.env`:

```env
VITE_API_BASE_URL=http://localhost:5000/api/v1
```

## Run

```bash
npm run dev
```

## Build

```bash
npm run build
```
